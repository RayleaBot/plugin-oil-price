name: plugin

on:
  pull_request:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:
    inputs:
      release_tag:
        description: Existing v* tag to publish; empty runs validation only
        required: false
        type: string

permissions:
  contents: read

env:
  RAYLEABOT_SDK_REF: sdk/go/v0.4.0
  GO_VERSION: 1.26.6

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout plugin
        uses: actions/checkout@v4

      - name: Checkout pinned RayleaBot SDK
        uses: actions/checkout@v4
        with:
          repository: RayleaBot/RayleaBot
          ref: ${{ env.RAYLEABOT_SDK_REF }}
          path: .rayleabot/core

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Link checked-out SDK
        shell: bash
        run: |
          set -euo pipefail
          cat > go.work <<EOF
          go ${GO_VERSION}

          use (
            .
            ./.rayleabot/core/sdk/go
          )
          EOF

      - name: Test Go plugin
        run: go test -race ./...

  build:
    if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
    needs: test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: [windows-x64, linux-x64, macos-arm64]
    steps:
      - name: Checkout plugin
        uses: actions/checkout@v4

      - name: Checkout pinned RayleaBot SDK
        uses: actions/checkout@v4
        with:
          repository: RayleaBot/RayleaBot
          ref: ${{ env.RAYLEABOT_SDK_REF }}
          path: .rayleabot/core

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Link checked-out SDK
        shell: bash
        run: |
          set -euo pipefail
          cat > go.work <<EOF
          go ${GO_VERSION}

          use (
            .
            ./.rayleabot/core/sdk/go
          )
          EOF

      - name: Build artifact
        env:
          RAYLEA_PLUGIN_BUILD_USE_WORKSPACE: '1'
        run: go run github.com/RayleaBot/RayleaBot/sdk/go/cmd/raylea-plugin build-go --plugin . --backend ./cmd/oil-price --target "${{ matrix.target }}" --out dist

      - name: Upload platform package
        uses: actions/upload-artifact@v4
        with:
          name: plugin-${{ matrix.target }}
          path: dist/*.zip
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(inputs.release_tag, 'v'))
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      RELEASE_TAG: ${{ inputs.release_tag || github.ref_name }}
    steps:
      - name: Download platform packages
        uses: actions/download-artifact@v4
        with:
          pattern: plugin-*
          path: release-assets
          merge-multiple: true

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create -R "${GITHUB_REPOSITORY}" "${RELEASE_TAG}" release-assets/*.zip --generate-notes --verify-tag
